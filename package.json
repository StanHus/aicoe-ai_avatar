{
  "name": "hedra-avatar-app",
  "version": "0.1.6",
  "scripts": {
    "start-app": "cd frontend && pnpm install && pnpm dev",
    "start-agent": "cd backend && pip install -r requirements.txt && python avatar_service.py start"
  }
}
